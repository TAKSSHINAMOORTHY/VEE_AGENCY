# Expense Compass

Track business bills and personal expenses.

## Run locally

- Install deps: `npm install` (or `bun install`)
- Start dev server: `npm run dev`
- Build: `npm run build`
- Preview build: `npm run preview`
